<% provide :title, @user.name %>
<div class="row">
  <aside class="col-md-2">
    <section class="user_info" id="user-info">
      <%= gravatar_for @user %>
      <p id="user-name">
        <%= @user.name %><br>
        Learned <%= pluralize Word.learned_words(current_user).count, "word" %>
      </p>
    </section>

    <section>
      <%= render 'shared/stat' %>
    </section>

    <section>
        Learned <%= pluralize(@learned_words_amount, "Word") %>
      </section>

    <section>
      <%= render 'shared/follow_form' if logged_in? %>
    </section>
  </aside>
  <div class="col-md-8">
    <section class="activity">
      <%= render @activities %>
    </section>
  </div>
</div>
